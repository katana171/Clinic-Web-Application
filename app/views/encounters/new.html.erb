<h1>Add a new Encounter</h1>
<%= form_for @encounter, :url => { :action => "create" } do |f| %>
 <%= render partial: "/layouts/encounter_information", locals: { f: f } %>
 <br>
 <br>
 <br>
 <%= f.submit 'Create Encounter', :name => 'create encounter' %>
 <%= f.submit 'Cancel', :name => 'cancel' %>
<% end %>
